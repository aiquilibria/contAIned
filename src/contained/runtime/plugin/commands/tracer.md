---
description: Query the contAIned tracer (session history, audit log, file changes)
---

Use the `contained-tracer` MCP tools (`get_schema` then `query_tracer`) to answer
the following question. "Tasks" here refers to contAIned agent sessions recorded in
the tracer database — not Claude's internal task management.

$ARGUMENTS
