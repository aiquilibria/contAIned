---
name: tracer
description: Query the contAIned tracer database for session history, audit log, and file changes. Use when asked about past agent sessions, what files changed, what commands were run, or any audit events.
---

Use the `contained-tracer` MCP tools (`get_schema` then `query_tracer`) to answer
the question. "Tasks" here refers to contAIned agent sessions recorded in
the tracer database — not Claude's internal task management.

$ARGUMENTS
