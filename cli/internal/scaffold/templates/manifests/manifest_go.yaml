# contAIned repo manifest — Go ecosystem
#
# Commit this file to .contAIned_manifest.yaml in your repository root.
# Only runtime.docker.toolchains and policy.qa.checks are permitted here;
# all policy settings are owned by the Mainlined or operator manifest.
#
# `contained init` merges this file on top of the operator manifest at
# image build time. Any change here triggers an image rebuild.

runtime:
  docker:
    toolchains:
      go: "1.22.5"   # pin your project's Go version here

policy:
  qa:
    checks:
      - name: test
        command: [go, test, ./...]
        when_changed: ["*.go"]
      - name: vet
        command: [go, vet, ./...]
        when_changed: ["*.go"]
