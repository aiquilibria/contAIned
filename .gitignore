# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Editors ───────────────────────────────────────────────────────────────────
.idea
.idea/
.vscode/
*.swp
*.swo

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
.env
.coverage
coverage.xml

# ── Node ──────────────────────────────────────────────────────────────────────
node_modules/

# ── Git worktree infrastructure ───────────────────────────────────────────────
# When /workspace is a git worktree these files appear at the repo root.
HEAD
config
/hooks
objects
refs

# ── contAIned — control plane ─────────────────────────────────────────────────────
# The .contAIned/ directory contains hooks, policy, and audit logs managed by contAIned.
# Keep it out of version control.
.contAIned/
.slash/
docs/internal/
.claude/*
pytest-of-agent*

# ── contAIned — container-injected dotfiles ───────────────────────────────────
# Empty stub files written by the contAIned runtime at container start.
.bash_profile
.bashrc
.gitconfig
.gitmodules
.mcp.json
.profile
.ripgreprc
.zprofile
.zshrc
# contAIned — shared images (operator drop zone)
.images/
